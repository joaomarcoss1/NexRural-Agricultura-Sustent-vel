export default function Modal({item,close}){if(!item)return null;return <div className="modal-bg" onClick={close}><div className="modal-box" onClick={e=>e.stopPropagation()}><button className="close-btn" onClick={close}>×</button><div className="modal-header"><span className="resource-tag">{item.categoria}</span><h3>{item.nome}</h3></div><p>{item.descricao}</p><h4>Benefícios</h4><ul>{item.beneficios.map((b,index)=><li key={`${item.nome}-${index}`}>{b}</li>)}</ul></div></div>}