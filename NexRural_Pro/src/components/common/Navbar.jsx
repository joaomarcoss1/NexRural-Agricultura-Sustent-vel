import {Link} from "react-router-dom";
import {useContext} from "react"; import {AppContext} from "../../context/AppContext";
export default function Navbar(){const {dark,setDark}=useContext(AppContext);const links=[{to:"/",label:"Início"},{to:"/sobre",label:"Sobre"},{to:"/recursos",label:"Recursos"},{to:"/consulta",label:"Consulta"},{to:"/financeiro",label:"Financeiro"},{to:"/contato",label:"Contato"}];return <nav className="topbar"><div className="container nav-inner"><div className="brand-wrap"><span className="brand-mark">N</span><span className="brand-text">NexRural</span></div><div className="nav-links">{links.map(item=><Link key={item.to} className="nav-link" to={item.to}>{item.label}</Link>)}</div><button onClick={()=>setDark(!dark)} className="theme-toggle">{dark?"☀️ Claro":"🌙 Escuro"}</button></div></nav>}