import dados from "../data/dados.json"; import {useState} from "react"; import ResourceCard from "../components/cards/ResourceCard"; import Modal from "../components/common/Modal";
export default function Recursos(){const [q,setQ]=useState(""),[sel,setSel]=useState(null);return <main className="container p-4"><input className="form-control" placeholder="Pesquisar compostagem..." onChange={e=>setQ(e.target.value)}/><div className="row mt-3">{dados.filter(d=>d.nome.toLowerCase().includes(q.toLowerCase())).map(d=><div className="col-md-4 mb-3"><ResourceCard item={d} click={setSel}/></div>)}</div><Modal item={sel} close={()=>setSel(null)}/></main>}