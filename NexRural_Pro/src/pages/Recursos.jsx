import dados from "../data/dados.json"; import {useState} from "react"; import ResourceCard from "../components/cards/ResourceCard"; import Modal from "../components/common/Modal";
export default function Recursos(){const [q,setQ]=useState(""),[sel,setSel]=useState(null);const filtered=dados.filter(d=>d.nome.toLowerCase().includes(q.toLowerCase())||d.categoria.toLowerCase().includes(q.toLowerCase()));return <main className="page-shell compact-page"><div className="container"><section className="content-panel"><div className="search-header"><div><span className="eyebrow">Biblioteca temática</span><h1>Recursos sustentáveis</h1></div><input className="search-field" value={q} onChange={e=>setQ(e.target.value)} placeholder="Pesquisar por recurso ou categoria..."/></div><div className="resource-grid">{filtered.map(d=><ResourceCard key={d.nome} item={d} click={setSel}/>)}</div></section></div><Modal item={sel} close={()=>setSel(null)}/></main>}