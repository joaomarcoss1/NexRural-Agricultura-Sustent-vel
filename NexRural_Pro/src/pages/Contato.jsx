import {useState} from "react"; import {validate} from "../utils/validators";
export default function Contato(){const [v,setV]=useState({nome:"",email:"",mensagem:""}),[m,setM]=useState("");return <main className="page-shell compact-page"><div className="container"><section className="contact-shell"><div className="contact-copy"><span className="eyebrow">Fale com a gente</span><h1>Conecte ideias, pessoas e práticas sustentáveis.</h1><p>Quer apoiar projetos rurais, trocar conhecimento ou colaborar com o NexRural? Escreva para a gente.</p></div><form className="contact-form" onSubmit={e=>{e.preventDefault();setM(validate(v)?"Mensagem enviada com sucesso.":"Verifique os campos preenchidos.")}}>{["nome","email","mensagem"].map((x,index)=><div key={x} className="field-wrap"><label>{x}</label>{x==="mensagem"? <textarea rows="5" value={v[x]} onChange={e=>setV({...v,[x]:e.target.value})}/>:<input value={v[x]} onChange={e=>setV({...v,[x]:e.target.value})}/>}</div>)}<button className="primary-btn" type="submit">Enviar mensagem</button></form></section>{m && <p className="status-message">{m}</p>}</div></main>}